import styled from "styled-components";

export const Titles = styled.h3`
  font-weight: 700;
  font-size: 16px;
  line-height: 30px;
`;

export const Subtitles = styled.label`
  font-weight: 400;
  font-size: 12px;
  line-height: 22px;
`;

export const Texts = styled.p`
  color: var(--gray-Texts);

  font-weight: 400;
  font-size: 12px;
  line-height: 22px;
`;

export const Values = styled.p`
  color: var(--grey-4);

  font-weight: 400;
  font-size: 12px;
  line-height: 22px;
`;

export const ValuesTotal = styled.p`
  color: var(--color-primary);

  font-weight: 700;
  font-size: 16px;
  line-height: 30px;
`;

export const Example = styled.p`
  color: var(--grey-Example);

  font-weight: 400;
  font-size: 12px;
  line-height: 20px;
`;

export const TextEmpyt = styled.h2`
  color: var(--gray-Texts);
`;
